import ReactDOM from "react-dom/client";

import LoginWithLocalStorage from "./Auth/LoginWithLocalStorage";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<LoginWithLocalStorage />);
